const badlyFormatted = 'intentional biome format violation'
const notANumber: number = "intentional type error for reviewdog (drop this tree before merging to main)";

export { badlyFormatted, notANumber };
